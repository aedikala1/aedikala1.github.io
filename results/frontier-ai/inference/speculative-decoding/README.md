# Speculative Decoding — results

Store lightweight evidence for the public write-up here:

- a machine-readable summary of each run
- small tables and plots
- evaluation fixtures or prompts where safe
- interpretation notes tied to exact run IDs
- failures and negative results

Do not commit secrets, private datasets, model weights, or large generated artifacts. Link those from this README when needed.
